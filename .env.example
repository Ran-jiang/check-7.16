# CCitecheck 环境变量（唯一配置文件）— 复制为 .env 并填入实际值。
# 所有变量均为可选：不配置时仅使用本地法条库，不做语义核查。

# 千问语义核查。仅在使用 --semantic-check 时必需。
DASHSCOPE_API_KEY=
QWEN_MODEL=qwen3.7-max
QWEN_BASE_URL=https://llm-qs6teo3293en0sk8.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

# 北大法宝回退与案号核验。本地法条库未命中时使用。
PKULAW_ACCESS_TOKEN=
PKULAW_MCP_GATEWAY=https://apim-gateway.pkulaw.com
